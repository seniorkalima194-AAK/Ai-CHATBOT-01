# Optional Docker Compose configuration. Copy this file to .env before
# starting Docker if you want to choose a different model or host ports.
OLLAMA_MODEL=gemma4:latest
FRONTEND_PORT=5173
BACKEND_PORT=8000
